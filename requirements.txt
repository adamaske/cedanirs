# Runtime dependencies (kept in sync with [project].dependencies in pyproject.toml).
# For development install the package with extras instead:  pip install -e .[dev]
numpy>=1.23
scipy>=1.9
xarray>=2023.1
pandas>=1.5
matplotlib>=3.6
networkx>=3.0
